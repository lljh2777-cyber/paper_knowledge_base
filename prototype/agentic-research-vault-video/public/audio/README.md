# Audio placeholder

The composition renders without audio. Optional production audio can be added here later:

- `soundtrack.wav`: restrained cinematic electronic bed, 90 seconds.
- cue effects aligned with `src/data/audioCues.ts`.

Keep all render-time audio local and update the composition explicitly before enabling it.
